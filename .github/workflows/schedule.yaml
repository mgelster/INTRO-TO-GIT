name: Schedule Workflow
on:
  schedule:
        - cron: '*/5 * * * *'
jobs:
    schedule-job:
          runs-on: ubuntu-latest
          steps:
            - name: Print Schedule triggered
              run: echo "This workflow runs every 5 minutes on a schedule"