name: First Workflow

on:
  push:
    branches:
      - main
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout
          uses: actions/checkout@v4.2.2
          
        - name : Print Hello World
          run: echo "Hellow Wrold!"